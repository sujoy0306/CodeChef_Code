#include<iostream>
#include<cstdlib>
using namespace std;
int main(){
	int t;
	cin>>t;
	while(t--){
		long long int n,*a,*b,i,j,f[3],l[3],val;
		cin>>n;
		bool *c;
		c=(bool*)malloc(n*sizeof(bool));
		a=(long long  int*)malloc(n*sizeof(long long int));
		b=(long long  int*)malloc(n*sizeof(long long int));
		for(i=0;i<n;i++){
			cin>>b[i];
		}
		for(i=0;i<n;i++){
			cin>>a[i];
		}
		int fl=0;
		f[0]=a[0];
		f[1]=a[0]+b[0];
		f[2]=a[0]+b[1];
		for(i=1;i<n;i++){
			if(i==1){
				if(f[0]<a[1]+b[0])
					break;
				else{
					
				
	}
	return(0);
}
		
